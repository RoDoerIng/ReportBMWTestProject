\ProvidesPackage{mystyle}

%% Localisation
\usepackage[ngerman]{babel}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

%% Font
\renewcommand*{\familydefault}{\sfdefault}
\renewcommand*{\rmdefault}{\familydefault}   %% optional if some packages change font family to roman
\usepackage{inconsolata}  %% set ttfamily font to consolas
\usepackage{helvet}

% Additional Symbols
\usepackage{textcomp}

%% Enumeration
\usepackage{enumitem}

%% Spacing
\usepackage{setspace}

%% Modular Documents
% https://en.wikibooks.org/wiki/LaTeX/Modular_Documents
% subfiles are no longer called by \input or \include but by using \subfile
\usepackage{subfiles}

%% Colors
\usepackage{xcolor} 		% Farboptionen
%\definecolor{hellgelb}{rgb}{1,1,0.8}
\definecolor{bluekeywords}{rgb}{0,0,1}
\definecolor{greencomments}{rgb}{0,0.5,0}
\definecolor{redstrings}{rgb}{0.64,0.08,0.08}
\definecolor{xmlcomments}{rgb}{0.5,0.5,0.5}
\definecolor{types}{rgb}{0.17,0.57,0.68}

\definecolor{red}{rgb}{0.6,0,0} % for strings
\definecolor{blue}{rgb}{0,0,0.6}
\definecolor{green}{rgb}{0,0.8,0}
\definecolor{cyan}{rgb}{0.0,0.6,0.6}
\definecolor{terminal_gray}{rgb}{0.9,0.9,0.9}

%% SCL specific colors
\definecolor{scl_blue_keywords}{rgb}{0,0,1}
\definecolor{scl_yellow_keywords}{rgb}{0.5,0.5,0}
\definecolor{scl_petrol_comments}{rgb}{0,0.5,0.5}
\definecolor{scl_pink_strings}{rgb}{1,0,1}

%% Physical units
\usepackage[binary-units = true]{siunitx}
\DeclareSIUnit{\Bit}{bit}

%%Captions
\usepackage{caption}

%% Lisitings
\usepackage{listings}
\renewcommand{\lstlistingname}{Quellcode}
\makeatletter
\lst@InstallKeywords k{attributes}{attributestyle}\slshape{attributestyle}{}ld
\makeatother
\lstloadlanguages{% Check Dokumentation for further languages ...
C,
C++,
csh,
Java,
XML
}

\lstdefinestyle{Terminal}{
	backgroundcolor=\color{black},  %terminal_gray, darkgray
	basicstyle=\ttfamily\color{terminal_gray}  %black, white
}

\lstdefinestyle{cSharp}{
	language=csh,
	basicstyle=\scriptsize\ttfamily,
	numbers=left,
	numberstyle=\tiny,
	numbersep=5pt,
	tabsize=2,
	extendedchars=true,
	captionpos = t,
	breaklines=true,
	breakatwhitespace=true,
	%escapeinside={(*@}{@*)},
	stringstyle=\color{redstrings},
	showspaces=false,
	showtabs=true,
	xleftmargin=17pt,
	frame=lines,		%bottom and top lines
	framexleftmargin=17pt,
	framexrightmargin=5pt,
	framexbottommargin=4pt,
	commentstyle=\color{greencomments},
	morecomment=[l]{//}, %use comment-line-style!
	morecomment=[s]{/*}{*/}, %for multiline comments
	showstringspaces=false,
	morekeywords={  abstract, event, new, struct,
	                as, explicit, null, switch,
	                base, extern, object, this,
	                bool, false, operator, throw,
	                break, finally, out, true,
	                byte, fixed, override, try,
	                case, float, params, typeof,
	                catch, for, private, uint,
	                char, foreach, protected, ulong,
	                checked, goto, public, unchecked,
	                class, if, readonly, unsafe,
	                const, implicit, ref, ushort,
	                continue, in, return, using,
	                decimal, int, sbyte, virtual,
	                default, interface, sealed, volatile,
	                delegate, internal, short, void,
	                do, is, sizeof, while,
	                double, lock, stackalloc,
	                else, long, static,
	                enum, namespace, string,
									partial, var, get, set},
	keywordstyle=\color{bluekeywords},
	moreattributes={	String, Console, Assembly,
										MyCustomClass, Result, ResultPrinter,
										SampleAttribute, Test, Person,
										ClockTowerEventArgs, ClockTower}, % etc...
	attributestyle = \color{types}, % (for instance)
	%identifierstyle=\color{red}
}


\lstdefinelanguage{SCL}
{
sensitive=true
}

\lstdefinestyle{sclStyle}{
	% float=tp,
	% floatplacement=tbp,
	language=SCL,
	basicstyle=\scriptsize\ttfamily,
	numbers=left,
	numberstyle=\tiny,
	numbersep=5pt,
	tabsize=2,
	captionpos = t,
	stringstyle=\color{scl_pink_strings},
	commentstyle=\color{scl_petrol_comments},
	classoffset=1,% starting a new class
	morekeywords = {
	CONFIGURATION,TASK,INTERVAL,
	VAR_GLOBAL,END_VAR,VAR_INPUT,VAR_OUTPUT, VAR_IN_OUT,VAR_TEMP,
	CONST,END_CONST,
	AT,OF,VOID,
	BOOL,INT,DINT,STRING,CHAR,
	STRUCT,END_STRUCT,TYPE,END_TYPE,
	FUNCTION,END_FUNCTION,
	IF,THEN,ELSE,END_IF,ELSIF,
	FOR,TO,BY,DO,END_FOR,
	RETURN}, 
	%alsoletter={=,+, -, *, :, <, >,(,),#},
	keywordstyle=\color{scl_blue_keywords},
	classoffset=2,% starting another class
	morekeywords={
	OR, AND,
	:,=,:=,<,>,
	},
	keywordstyle=\color{scl_yellow_keywords},
	classoffset=0, % restore to default class if more customisations...
	moreattributes={TRUE,FALSE	}, % etc...
	attributestyle = \color{scl_pink_strings}, % (for instance)
	morestring=[b]{"},
	morestring=[b]{'},
	morecomment=[l]{//},
	morecomment=[s]{(*}{*)},
	frame=lines,
	literate=%
	% {+}{{{\color{scl_yellow_keywords}+}}}1
	% {-}{{{\color{scl_yellow_keywords}-}}}1
	% {*}{{{\color{scl_yellow_keywords}*}}}1
	% {/}{{{\color{scl_yellow_keywords}/}}}1
	% {=}{{{\color{scl_yellow_keywords}=}}}1
	% {:=}{{{\color{scl_yellow_keywords}:=}}}1
	% {0}{{{\color{scl_pink_strings}0}}}1
	% {1}{{{\color{scl_pink_strings}1}}}1
	% {2}{{{\color{scl_pink_strings}2}}}1
	% {3}{{{\color{scl_pink_strings}3}}}1
	% {4}{{{\color{scl_pink_strings}4}}}1
	% {5}{{{\color{scl_pink_strings}5}}}1
	% {6}{{{\color{scl_pink_strings}6}}}1
	% {7}{{{\color{scl_pink_strings}7}}}1
	% {8}{{{\color{scl_pink_strings}8}}}1
	% {9}{{{\color{scl_pink_strings}9}}}1
}
	
\lstdefinelanguage{AWL}
{
sensitive=true
}

\lstdefinestyle{awlStyle}{
	language=AWL,
	basicstyle=\scriptsize\ttfamily,
	numbers=left,
	numberstyle=\tiny,
	numbersep=5pt,
	tabsize=2,
	stringstyle=\color{redstrings},
	commentstyle=\color{greencomments},
	morekeywords = {
	LD,ST,AND,JMPCN,JMP,CAL,LDN,ANDN,
	L,T}, 
	%alsoletter={=,+, -, *, :, <, >,(,),#},
	keywordstyle=\color{bluekeywords},
	morestring=[b]{"},
	morestring=[b]{'},
	morecomment=[l]{//},
	morecomment=[s]{(*}{*)},
	frame=lines
}
%%Drawing with LaTeX
\usepackage{./sty/tikz-uml}

%% Citations and Bibliography
\usepackage[hyphens]{url}
%\usepackage{natbib}
\usepackage{multibib}
\newcites{ldat}{Datenblätter}
\newcites{lonl}{Internetquellen}
\newcites{lpic}{Bildquellen}
\newcites{llit}{Literaturquellen}
\newcites{lothers}{Sonstige Quellen}

%% Tables
\usepackage{tabularx}		%% Tabellen mit etwas mehr Modifikationsm�glichkeiten
% \usepackage{array}
% \usepackage{multirow}
%\usepackage{booktabs}		%% Linien verschiedener Dicke in Tabelle
	\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}} % linksbündig mit Breitenangabe
	\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}} % zentriert mit Breitenangabe
	\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}p{#1}} % rechtsbündig mit Breitenangabe
	\newcommand{\lft}{\raggedright\arraybackslash} % Tabellenabschnitt linksbündig
	\newcommand{\cntr}{\centering\arraybackslash} % Tabellenabschnitt zentriert
	\newcommand{\rght}{\raggedleft\arraybackslash} % Tabellenabschnitt rechtsbündig
% make all tabularx use footnotesize
\let\oldtabularx\tabularx
\renewcommand{\tabularx}{\footnotesize\oldtabularx}
% \usepackage{ltxtable}		%% longtable mit tabularx-Formatierung aus externer Datei einbinden

%% Links in PDF document
% Verlinkungen im PDF-Dokument
\usepackage[
				pdftex,
				colorlinks=true,
        linkcolor=black,
        citecolor=black,
        filecolor=black,
        urlcolor=blue,
        bookmarks=true,
        bookmarksopen=true,
        bookmarksopenlevel=3,
        plainpages=false,
		pdfpagelabels=true]{hyperref}
\hypersetup{final}		%creates links even in draft mode

%% Graphics
%\usepackage{graphics}
\usepackage{floatflt}
\usepackage{float}
%\usepackage{picinpar}
\usepackage{graphicx}
%\usepackage{wrapfig} %Bilder mit \begin{wrapfigure} {l} {5cm} einbinden, um Text herum schreiben zu können (linksbündig) (Bild 5cm breit)
\usepackage[export]{adjustbox}
\usepackage{subfig}
\usepackage{pgffor}
\usepackage[abs]{overpic}		% Elemente auf Bildern positionieren
\usepackage{epstopdf}		% eps-Dateien als Bilder einbinden
\usepackage{svg}			% include svg images
\usepackage{pdfpages} 

\usepackage[
%justification=centering
]{caption}

\addto\captionsngerman{\renewcommand{\figurename}{Abb.}}
\graphicspath{{./img/}}

%% Physical Units
\usepackage{units}
\usepackage[]{siunitx}
\DeclareSIUnit{\Bit}{bit}
\DeclareSIUnit\cars{Autos}

%% Abbreviations
\usepackage[intoc]{nomencl}
% Befehl umbenennen in abbrev
\let\abbrev\nomenclature
% Überschrift
\renewcommand{\nomname}{Abkürzungsverzeichnis}
\makenomenclature

%% Create index
% \usepackage{makeidx}
% \makeindex
% makeindex document.nlo -s nomencl.ist -o document.nls

%%%%%%%%%%%%%%%%%%%%%%
%%%% NEW COMMANDS %%%%
%%%%%%%%%%%%%%%%%%%%%%

\newcommand{\bild}[4]{
	\begin{figure}[#1]   %htbp!H https://en.wikibooks.org/wiki/LaTeX/Floats,_Figures_and_Captions
		\begin{center}
			\includegraphics[#2]{#3}
			\captionsetup{font=small}
			\caption{#4}
			\label{fig:#3}
		\end{center}
	\end{figure}
}


%% Superscript Subscript
\newcommand{\ti}[1]{\textsubscript{#1}} 		% tiefstellen im Text mit \t
\newcommand{\ho}[1]{\textsuperscript{#1}} 	% hochstellen im Text mit \h

%% insert empty row
\newcommand{\leer}{\par \bigskip \noindent}

%% Number with unit
\newcommand{\einh}[2]{\SI[detect-all, output-decimal-marker = {,}]{#1}{#2}}

%% Source Code Paths
\newcommand{\src}{src}
\newcommand{\extsrc}{}
